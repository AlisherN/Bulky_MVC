@model List<Product>

<div class="container">
    <div class="row pt-4 pb-4">
        <div class="col-6">
            <h2 class="text-dark">Product List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn-dark">
                Create <i class="bi bi-plus-circle"></i>
            </a>
        </div>
    </div>
    <table id="tblData" class="table table-bordered table-striped" style="width: 100%">
        <thead>
            <tr>
                <th>
                    Product title
                </th>
                <th>
                    Product ISBN
                </th>
                <th>
                    List Price
                </th>
                <th>
                    Product Author
                </th>
                <th>
                    Category
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        @*Quyidagi kod MVCda ishlayotgan edi. Keyin DataTable plugin yordamida
        ajax bilan olinadigan qilingandan keyin kerak bo'lmadi*@
        @*<tbody>

            
            @foreach (Product product in Model.OrderBy(o => o.Id))
            {
                <tr>
                    <td>@product.Title</td>
                    <td>@product.ISBN</td>
                    <td>@product.Author</td>
                    <td>@product.Category.Name</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Product" asp-action="Upsert" asp-route-id="@product.Id" class="btn btn-dark mx-2">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>
                            <form method="post" asp-controller="Product" asp-action="Delete" asp-route-id="@product.Id">
                                <button class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>*@
    </table>
</div>

@section Scripts{
`<script src="~/js/product.js"></script>
}